<!DOCTYPE html>
<html lang="en">
<%- include('./components/head.ejs') %>

<body>

    <%-  include('./components/navbar.ejs') %>

    <%- '<h1>Hello world</h1>' %>
    <% if(blogs.length) { %>
        <% blogs.forEach(blog => { %>
            <h1><%= blog.title %></h1>
            <%= blog.intro %>
        <% }) %>
    <% } else { %>
        <h3>No Blogs found.</h3>
    <% } %>

</body>
</html>